global:
  scrape_interval: 15s

scrape_configs:
  # Collector expose metric dạng Prometheus tại :8889 => prometheus là bên chủ động gọi đến collector để lấy metric
  - job_name: otel-collector
    static_configs:
      - targets: ["otel-collector:8889"]
